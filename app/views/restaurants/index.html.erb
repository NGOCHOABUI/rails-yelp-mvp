<h1>Restaurants</h1>
<ul>

    <% @restaurants.each do |rest| %>
    <li><%= rest.name%>--<%= rest.address%>--<%= rest.phone_number%>--<%= rest.category%>
    <%= link_to "Show", restaurant_path(rest) %>
    </li>

    <% end %>
</ul>
<%= link_to "Add new restaurant", new_restaurant_path %>
